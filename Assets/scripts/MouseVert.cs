using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MouseVert : MonoBehaviour
{

    //something
    public enum RotationAxes
    {
        MouseXAndY = 0,
        MouseX = 1,
        MouseY = 2
        
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
